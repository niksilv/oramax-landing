export const metadata = {
  title: 'Contact  Oramax',
  description: 'Contact page placeholder for build.',
};

export default function ContactPage() {
  return (
    <main style={{padding:'24px', fontFamily:'system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif'}}>
      <h1 style={{fontSize:'20px', marginBottom:'12px'}}>Contact</h1>
      <p>This is a placeholder page so the Next.js build can succeed.
      Replace this file with your real Contact page when ready.</p>
    </main>
  );
}
